#mandelbrot.py
#A script containing mandelbrot and hyperbrot objects, with rendering and plotting methods. 
import numpy as np

class mandelbrot:
    def __init__(self, name, resolution):
        self.name = name
        self.resolution = resolution

    #Function to render mandelbrot set with resolution self.resolution and 
    #def render(self, a):
    

